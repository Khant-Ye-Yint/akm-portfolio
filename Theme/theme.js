import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
	colors: {
		primary: '#E072A4',
		background: '#FFF4F4',
		grey: '#676869',
		black: '#31393C',
	},
	fonts: {
		dosis: 'Dosis',
		saira: 'Saira',
	},
});

export default theme;
